The client side is based on dashboard, login, registration
Ignore the UI cus i suck at that but the backend works solid.
You can change the db name/details at db_client.php also you can find the working sql version at the main respo inside the sql folder.
